import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionicvueV5-login-register-form-validation-appv1',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
